# Obsidian
.obsidian/
.trash/

# Private notes — local-only, never pushed
private/
!private/.gitkeep

# Personal audit system — content syncs via git but is excluded from the Quartz site
# (see ignorePatterns in quartz.config.ts and deploy-quartz.yml)

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
